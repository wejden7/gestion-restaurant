import React from "react";
import './BtnMenu.style.scss'
export default function BtnMenu({ onClick }) {
  return (
    <button
      className="btn-menu"
      onClick={onClick}
    >
    </button>
  );
}
